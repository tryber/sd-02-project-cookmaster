<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Cookmaster - Home</title>
</head>
<body>
  <nav>
    <h3>Cookmaster</h3>
    <h4><%= recipe.userName%></h4>
    <a href="/logout">Logout</a>
  </nav>
  <h1><%= recipe.recipeName%></h1>
  <h3>Enviada por <%= recipe.userName%></h3>
  <% if (recipe.userId === userId) { %>
    <a href=<%= `/recipes/${recipe.recipeId}/edit` %>>
      <button type="button">Editar Receita</button>
    </a>
    <a href=<%= `/recipes/${recipe.recipeId}/delete` %>>
      <button type="button">Excluir Receita</button>
    </a>
  <% } %>
  <h2>Ingredientes</h2>
  <ul>
    <% recipe.ingredients.split(',').forEach((ingredient) => { %>
     <li><%= ingredient %></li>
    <% }) %>
  </ul>
  <h2>Modo de Preparo</h2>
  <p><%= recipe.instructions %></p>
</body>
</html>
