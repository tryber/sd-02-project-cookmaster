<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Cookmaster - Nova Receita</title>
  </head>

  <body>
    <h1>Nova Receita</h1>
    <form action="<%= recipe.id ? `/recipes/${recipe.id}` : '/recipes/new' %>" method="POST">
      <label for="name" ">Nome da Receita: </label>
      <input type="text" name="name" id="name" value="<%= recipe.name || '' %>"/><br />
      <label for="ingredients">Ingredientes: </label>
      <input type="text" name="ingredients" id="ingredients" value="<%= recipe.ingredients || ''%>"/><br />
      <label for="instructions">Modo de Preparo: </label>
      <input type="text" name="instructions" id="instructions" value="<%= recipe.instructions || ''%>"/><br />
      <button type="submit">Salvar Receita</button>
    </form>
    <button onclick="window.location.pathname='/'">Voltar</button>
  </body>
</html>
