<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Exclusão da Receita</title>
</head>

<body>
  <div>
    <h1>Excluir Receita</h1>
    <form action=<%= `/recipes/${id}/delete` %> method="POST">
      <label for="password">Digite sua senha para confirmar a exclusão</label>
      <input type="password" id="password" name="password">
      <button type="submit">Excluir Senha</button>
    </form>
    <% if(wrongPass) { %>
    <h3>Senha incorreta. Por favor, tente novamente.</h3>
    <% } %>
  </div>
</body>
