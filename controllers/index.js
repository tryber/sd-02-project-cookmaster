const userController = require('./userController');
const rootController = require('./rootController');
const recipesIdController = require('./recipesIdController');

module.exports = {
  userController,
  rootController,
  recipesIdController,
};
